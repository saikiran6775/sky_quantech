# Dependencies
node_modules/

# Local tooling state (session logs, caches) — not part of the project
.remember/
.gstack/
# OS
.DS_Store
# The original Stitch export archive. Its unpacked contents are committed as
# stitch_sky_quantech_ai_website/, so the zip is redundant weight.
*.zip
Drone_video.MP4
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
# testing
/coverage
# next.js
/.next/
/out/
# production
/build
# misc
*.pem
# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
# env files (can opt-in for committing if needed)
.env*
# vercel
.vercel
# typescript
*.tsbuildinfo
next-env.d.ts
